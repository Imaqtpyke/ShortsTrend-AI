import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.contentgenerator.app',
    appName: 'Content Generator',
    webDir: 'dist'
};

export default config;
